<div class="series">

        {{#each data}}
        <div class="sub-div">

            <div class="images">
                <img src="{{imagem}}">
            </div>

            <div class="titulo">{{titulo}}
            </div>

            <div class="genero">
            {{generos}}
            </div>
        </div>
        {{/each}}

</div>